/**
 * Tailwind v4 is CSS-first (`@import "tailwindcss"` + `@theme` in
 * `apps/web/app/globals.css`) — there is no JS config object to share.
 * This file exists as the one place design tokens get exported for
 * non-Tailwind consumers (e.g. a chart library's color scale). Populated
 * in Phase 3B alongside the visual system; intentionally empty until then.
 */
export const tokens = /** @type {const} */ ({});
