{
  "name": "@netverdict/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic measurement core. Runs inside a browser Worker or headless Node — zero framework dependencies (§2.2/§2.14 of the build brief).",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b --pretty",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "measure": "tsx bin/measure.ts"
  },
  "dependencies": {
    "@netverdict/contracts": "0.1.0"
  },
  "devDependencies": {
    "tsx": "4.23.1",
    "typescript": "5.9.3"
  }
}
